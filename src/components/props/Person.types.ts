export type Name = {
    first: string,
    last: string
}

export type PersonType = {
    name: {
        first: string,
        last: string
    }, // PersonList Array in Object
}

// 중복되는 내용을 줄이기 위함